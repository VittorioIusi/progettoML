# Copia questo file in `.env` e inserisci la tua chiave.
# `.env` NON deve mai essere committato (vedi .gitignore).
#
# Token di licenza Prior Labs, necessario SOLO per il primo download
# dei pesi di TabPFN da HuggingFace. Generalo (gratis) su:
#   https://ux.priorlabs.ai
TABPFN_TOKEN=
