Fixed `fit()` hanging forever when stratified row subsampling allocates a class more slots than it has rows (e.g. an ultra-rare class with `SUBSAMPLE_SAMPLES` set); such classes are now minimally oversampled instead.
