Fixed `norm_and_kdi` returning a feature schema that undercounts the output columns: the FeatureUnion emits two columns per input column, so the schema and `num_added_features` under-reported, letting the ensemble's feature-budget planning silently exceed `max_features_per_estimator`.
