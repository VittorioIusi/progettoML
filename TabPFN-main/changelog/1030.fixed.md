Fix `save_fitted_tabpfn_model`/`save_fit_state` moving the live estimator's bar distribution modules to CPU, which broke subsequent `predict` calls (e.g. `output_type="median"`/`"quantiles"`) on CUDA/MPS devices.
